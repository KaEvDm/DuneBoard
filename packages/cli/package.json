{
  "name": "@duneboard/cli",
  "version": "0.11.0",
  "private": true,
  "type": "module",
  "description": "DuneBoard command line workflow for agents and humans.",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "start": "tsx src/index.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@duneboard/core": "workspace:*",
    "commander": "^14.0.1",
    "yaml": "^2.8.0"
  }
}
