{
  "name": "@duneboard/core",
  "version": "0.6.0",
  "private": true,
  "type": "module",
  "description": "DuneBoard task schema, parser, validator, and graph index.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "yaml": "^2.8.0",
    "zod": "^3.25.36"
  }
}
